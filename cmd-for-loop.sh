#!/bin/bash
#This script will run a command for a certain number of iterations.
#START=`date +%s`
#DURATION=30
#while [ $(( $(date +%s) - $DURATION )) -lt $START ];

#1..3000 is the range
for i in {1..3000}
do
    #insert command here
    
done
echo "Done"
